apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: hello-world-dev
  namespace: argocd
  labels:
    app: hello-world
    environment: dev
    essesseff: "true"
    configenvrepoid: "{{REPOSITORY_ID}}"
spec:
  project: default
  source:
    repoURL: https://github.com/essesseff-hello-world-flask-template/hello-world-config-dev
    targetRevision: main
    path: .
    helm:
      valueFiles:
        - values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: essesseff-hello-world-flask-template
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - CreateNamespace=true
      - PrunePropagationPolicy=foreground
      - PruneLast=true
    retry:
      limit: 3
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
  revisionHistoryLimit: 10
  info:
    - name: hello-world
      value: "hello-world GitOps deployment managed by essesseff"
    - name: environment
      value: "dev"

